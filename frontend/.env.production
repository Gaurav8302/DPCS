# Production Backend URL
NEXT_PUBLIC_BACKEND_URL=https://dpcs.onrender.com

# NextAuth Configuration
NEXTAUTH_URL=https://dpcs-722m.vercel.app
NEXTAUTH_SECRET=production-secret-change-this-use-openssl-rand-base64-32

# Note: Update NEXTAUTH_SECRET with a strong random value
# Generate with: openssl rand -base64 32
# Or PowerShell: 
# $bytes = New-Object byte[] 32
# [System.Security.Cryptography.RandomNumberGenerator]::Create().GetBytes($bytes)
# [Convert]::ToBase64String($bytes)
